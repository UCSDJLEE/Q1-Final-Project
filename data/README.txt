Both training and test data available under `teams` directory
on DSMLP as size of those data are too large to reside on Github
remote repository.

Scripts/source codes under `src` directory have all filepaths
written as absolute path to avoid confusion in data loading process.

This file is to notify this to the grader for our Q1 project.
